services:
  - type: web
    name: greater-minds
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: HOST_PASSCODE
        sync: false
